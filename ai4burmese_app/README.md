# burmese_tutor app

The main project documentation now lives in the repo root:

- `../README.md`

Quick run from this folder:

```bash
chmod +x install_and_run.sh
./install_and_run.sh
```

The app starts at `http://127.0.0.1:5000`.
